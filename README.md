# pmanager
This Python script implements a secure password manager using encryption. It allows users to add, retrieve, generate, and delete passwords for various services via a command-line interface. A master password is required to access and manage the stored passwords.
